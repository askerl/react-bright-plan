# react-bright-plan

Project setup:

1. Clone or donwload
2. npm install
3. npm run serve

In case of any unmet dependencies o versioning issues:

1. npm uninstall webpack --save-dev
2. npm install webpack@2.1.0-beta.21 --save-dev
